# GreenThumb
A gardening application to help you give each plant species the special care it needs.

## Technologies
`React 
Redux 
Knex 
SQLite3 
PostgreSQL 
Webpack`

## Setup

After cloning repo
`npm install`

To run the app:
`npm run dev`

To run the server:
`npm start`
